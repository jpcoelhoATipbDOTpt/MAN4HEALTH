Man 4 Health - problema de atualiação de pluging:


*
*

As permissões de arquivo necessárias para importar os modelos estão faltando.

Você pode facilmente atualizar permissões adicionando o seguinte código no arquivo wp-config.php.

define( 'FS_METHOD', 'direct' );

*
*


No php.ini
  max_execution_time=3000
  memory_limit=512  
  max_input_time=900
  post_max_size=500M
  upload_max_filesize=720M

wp-config.php

 define('FS_METHOD','direct');
        define( 'WP_MEMORY_LIMIT', '500M' );